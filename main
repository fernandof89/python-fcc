# After defining a string, list, dict, tuple I can check the available functions using:
dir(variable)

#tuples cannot be modified, for temporary variables usually

#Extract content from a web, decoded for taking the bits into strings.
import urllib.request
fhand = urllib.request.urlopen('http://data.pr4e.org/romeo.txt')
for line in fhand:
    print(line.decode().strip())

#rjust - to justify to the right, uses the value of indexes and what to fill with.
a = 'This is a string'
a.rjust(10, 'x') 

#To avoid adding space at the end of a string
    if problem != problems[-1]:
      first += top + '    '
      second += bottom + '    '
      lines += line + '    '
      sumx += res + '    '
    else:
      first += top
      second += bottom
      lines += line
      sumx += res

#Two variables iteration
jjj = {'chuck' : 1, 'fred' : 3}
for aaa,bbb in jjj.item():
    print (aaa,bbb)
